export const getTaskEntities = ({ task }) => task.entities;

export const getTaskIds = ({ task }) => task.ids;

export const getGroupIds = ({ group }) => group.ids;

export const getGroupEntities = ({ group }) => group.entities;
